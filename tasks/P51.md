# P51 — Muzzle + impact sparks

**Builder:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §5.4 #5–7 · **Shipped:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Version:** `0.4.0-wave3`

---

## Gap (from P50)

Opening clash clamped `shootFlash` to **0.04** — muzzle invisible. Bolts traveled with no landing spark. Combat read from white `hitFlash` and HP chrome, not AoE2's muzzle → travel → impact chain.

---

## Changes

| Area | Result |
|---|---|
| **Muzzle** | Pooled `Spark` bursts (96 slots, ring reuse) at shooter on `spawnBolt`; per-civ atlas (`spark-muzzle-vespari` gold, `aurion` ice cyan, `voidmarked` spore). Opening `shootFlash` clash scale **0.42** (was 0.04). |
| **Impact** | Impact sparks on bolt hit (`stepBolts`) and melee (`tryStrike`); `spark-impact-*` sprites, ~2 ticks life. |
| **Bolts** | Team-colored travel unchanged; `Bolt.civ` added for impact tint. |
| **Perf** | No per-tick alloc on spark path; preallocated pool. |

**Not in scope (P52):** corpse dissolve, foot ellipses, HP-bar combat gate.

---

## Verify

| Check | Result |
|---|---|
| `npm run build` | Pass |
| Screenshot wait 3s (`critic/out/p51.png`) | Gold/cyan muzzle + impact sparks in exchange belt; Helion rank vs Kryos wrecks + mid gem intact |
| `npm run critic` local 5174 | p99 **3.0 ms**, avg 1.43 ms, **0** frames worse than 45 fps |
| Probe | `0.4.0-wave3`, 66 ents, tick 140, tableau pop 22/9 |

---

## Files

- `src/engine.ts` — `Spark`, `MAX_SPARKS`, `Bolt.civ`
- `src/sim.ts` — spark pool, `spawnSpark`, `stepSparks`, combat hooks
- `src/atlas.ts` — `drawSpark`, six spark atlas cells
- `src/render.ts` — spark draw pass; clash `shootFlash` 0.42
- `src/main.ts` — `VERSION = 0.4.0-wave3`
