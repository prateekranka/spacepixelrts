# P31 — Put workers in front of the flank house

**Builder:** Composer 2.5 · **Gap from:** `tasks/P30-critic.md`

## Problem

P30 parked camps beside the ranks but workers spawned at `cz-5.45` under the house billboard at `cz-5.15`, so zero worker silhouettes read in the opening frustum.

## Changes

- **Z stacking (Helion):** rank outer ~`cz-4.1` → workers + ore gem at `cz-4.75` → house at `cz-6.05`. Kryos mirrored on `+Z`.
- Workers pitched at `(cx - 0.9 + i*0.7, cz ± 4.75)`; ore/gas at `(cx + 0.9, cz ± 4.75)`; houses at `(cx, cz ± 6.05)`.
- Opening flank workers skip return-to-hall, `moveSeparate`, and steering while `tick < 240` — stay pitched and strike the node (extended gather reach).
- Worker billboard **1.55**; house **1.75**; fighters **1.08** unchanged.
- Helion worker atlas: gold pack kept; bright bone/white stripe + shoulder highlight so workers don’t read as hooded fighters.
- `openingFlankCampEnt()` vis band `|z-cz|` **4.4–6.4**.

## Verification

`critic/out/p31.png` — p99 **4.2 ms**, probe **0.2.6-wave1**, 61 ents. Three gold-packed Helion workers + ore gem + house visible between the green wing and the flank structure; ranks still exchanging fire. Deployed to https://spacepixelrts.pages.dev
