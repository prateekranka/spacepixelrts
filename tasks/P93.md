# P93 — full character roster (GAP 3)

## Done

Audited and rewrote every living `draw*Pix` in `src/sprites.ts`:

| Role | Change |
|---|---|
| **Fighter** | Kept reference silhouette; removed hive `circ()` trim (rect badges only). |
| **Worker** | Larger hunched ink form, plank-banded ore crate (not solid gold gem), skin grips on load, void drill path with hand on shaft. |
| **Scout** | Rebuilt as low hull + trapezoid dish on mast + rear exhaust stack — no floating dish orbs. |
| **Siege** | Tread base + turret + gun-metal cannon (stock/receiver/barrel/muzzle); removed MAG gold diamond and wheel orbs. |
| **Ravager** | Full torso/legs on ground, bone scythe shafts with blade rects (not SOL orb tips). |
| **Prism** | Larger crystal body, rect focus lens + beam weapon, stabilizer pods on stems — no `circ()` lens or MAG band. |
| **Shade** | Hood/cloak/feet + glowing eye + dagger with skin grip on hilt. |

`VERSION` → `0.8.2-art`.

## Verify

- `npx tsc --noEmit` ✓
- `npm run build` ✓
- `critic/out/p93.png` — opening tableau (hard reload of dev build)
- `critic/out/p93-atlas.png` — atlas + 4× role strip from live `sprites.ts`

## Least sure at opening zoom

1. **Worker** — plank crate still shares palette with the contested mid-map **ore gem node** (atlas diamond, out of P93 scope); at gather distance the load can still flash gem-like until the hunched legs/arms read.
2. **Prism** — only Kryos unique; not in the Helion opening rank. Hover + beam is clear in atlas strip but untested in live clash framing.

## Not in scope

- Ore/gas/solar **resource** gems (`src/atlas.ts`) — center gold diamond in tableau is the ore node prop, not a unit sprite.
- Buildings (P92), terrain (P91), HUD/VFX/sim/camera.
