# P55 — Idle-worker HUD pulse

**Builder:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §7.4 · **Shipped:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Version:** `0.4.3-wave3`

---

## Gap (from P54)

§9 #2 command feel — smallest missing ack layer: `#idlew` never pulsed when any team-0 Worker was `Ord.Idle`. AoE2's villager bell signals empire presence.

---

## Changes

| Area | Result |
|---|---|
| **Idle detection** | Each HUD frame: any alive team-0 Worker with `hp > 0` and `order === Ord.Idle` toggles `.pulse` on `#idlew`. |
| **Pulse VFX** | Gold border throb + scale/ glow keyframes (`idlew-pulse`, 1.05s loop). Removed when no idle workers. |
| **Click behavior** | Unchanged — finds first idle drone and recenters camera. |

**Not in scope:** opening/marshal restage, formation spread, combat SFX.

---

## Verify

| Check | Result |
|---|---|
| `npm run build` | Pass |
| Playwright: force one Worker `Ord.Idle` via `__STARHOLD_WORLD__` | `#idlew` has `.pulse` class |
| `npm run critic -- --url http://localhost:5174` | p99 < 22 ms |
| Opening tableau | Unchanged — gatherers busy, pulse off during clash window |
| Deploy | `spacepixelrts.pages.dev` ships `0.4.3-wave3` |

---

## Files

- `src/hud.ts` — idle-worker scan in `draw()`, `.pulse` CSS animation on `#idlew`
- `src/main.ts` — `VERSION = 0.4.3-wave3`
