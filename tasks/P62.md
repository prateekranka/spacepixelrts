# P62 — Screen-space attack pick (marshal peel)

**Builder:** Composer 2.5 · **Bar:** P58/P59 Attack-lock gap, `docs/ARCHITECTURE.md` pick · **Judged:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Probe:** `0.5.2-wave4`

**Harness:** `node scripts/p62-probe.mjs --url http://127.0.0.1:5174` (or live URL)  
**Output:** `critic/out/p62-probe.json`

---

## What changed

| Area | Result |
|---|---|
| **Enemy attack pick** | `orderAt` uses `closestEnemyScreen()` — project enemy foot, pointer within **28 CSS px** (`PICK_PX`, ARCHITECTURE pick). No more **1.4-tile** world disk that re-issued `Ord.Attack` on ground clicks near the clash belt. |
| **Resource gather** | `closestResource()` keeps modest world radius (**1.5**) but requires the pointer on the node sprite in screen space — empty ground over the mid gem no longer steals Move as Gather. |
| **Opening tableau** | Spawn / ceasefire / scripted `Ord.Attack` unchanged — clash still holds on its own. |
| **`World.issue`** | Unchanged — formation spread for multi-unit Move intact. |

---

## Playwright probes (5174, Chrome headless, tick < 240)

| Probe | Result |
|---|---|
| Opening 3 s | Helion **8**, Kryos **6**; version `0.5.2-wave4` |
| Right-click ground **(28, 40)** behind rank | `order` **Move** (`1`); `tx,tz` at click — **pass** |
| Right-click on living Kryos hull sprite | `order` **Attack** (`2`); `tid` matches hull — **pass** |
| `npm run critic` | p99 **3.0 ms**, **0** frames worse than 45 fps |

---

## Ship note (`0.5.2-wave4`)

- `src/input.ts` — `PICK_PX`, `closestEnemyScreen()`, `closestResource()`; `orderAt` screen picks only.
- `src/main.ts` — `VERSION = 0.5.2-wave4`.
- `scripts/p62-probe.mjs` — ground Move + hull Attack probes.
