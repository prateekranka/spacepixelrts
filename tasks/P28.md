# P28 — Fill opening frustum with a place

**Builder:** Composer 2.5 (partial) + orchestrator finish after `resource_exhausted` · **Gap from:** `tasks/P27-critic.md`

## Problem

P27 ranks passed; critic still saw ~75% dotted void. Dust tiles were a flat diamond stamp. Gems sat on the rim. No camp in frame.

## Changes

- Three dust variants + boulder rock tiles (`src/atlas.ts`); `buildMap` hashes Dust variants.
- Interior rocks, in-frustum gems `(cx±5, cz±4)`, wreck/vent props (`Tile.PropWreck` / `PropVent`).
- Forward Helion/Kryos houses + 3 gathering workers on the clash depth band.
- House/hall scale reduced so camps are not giant side silhouettes.
- Probe `0.2.3-wave1`. Hold-fire ranks unchanged.

## Verification

`critic/out/p28.png` — p99 **4.2 ms**. Palette no longer a single 50% purple (top share 22.6%). Ranks still countable. Deployed to https://spacepixelrts.pages.dev
