# VS4A Builder — rebuild Rift Guard core anatomy (strict TDD)

Read:
1. docs/VS4_RIFT_GUARD_REBUILD.md (frozen architecture replacement)
2. docs/VS1_COMBAT_ASSETS.md locked R3 contracts
3. current R3 commit 892d96c
4. tasks/SOL-vs4-r3-blind-gate.log final verdict/prescription
5. current drawRiftGuardCombat and row2 tests/QA

This is NOT R4 scale/value tuning. Do not change scales, rim, palette thresholds, shader, or any other
unit. Replace row2 core composition only.

Owned production ONLY:
- src/sprites.ts: `drawRiftGuardCombat` and any row2-local helper only
- tests/vs4-combat-assets.test.ts
- scripts/qa-vs4-combat-assets.mjs only to add row2 anatomy metrics/hashes
- tasks/VS4A-red.log (force-add)
No render/sim/shader/atlas layout/gameplay/AI/stats/HUD/main/input/content/terrain/dependencies.

Strict RED before source edits:
1. Compute and hardcode deterministic R3 SHA-256 hashes for every row0/1/3 dir/pose cell in the test.
2. Add row2 anatomy assertions against unchanged R3 (ignore amber/cream/ice/sky exterior rim colors).
3. Run; verify existing cabinet composition fails anatomy assertions; save full tasks/VS4A-red.log.

Analyze all authored directions after normalizing mirror dirs 3/4/5 back to 1/0/7. Per pose:
- core head region: a distinct connected 12x12 outer mass in x26..45/y16..27, with at least 80 core
  pixels and a 6px-wide ice visor; no shield core in head center x29..40/y16..27;
- torso region x25..46/y27..43: core alpha bbox >=18x16 and >=180 core pixels;
- left leg region x24..33/y42..51 and right leg x36..45/y42..51 each contain a >=6px-wide separate
  component/mass; center gap x33..36/y45..51 has zero non-rim core alpha;
- shield core fits one side x8..26/y19..49, has 16x29+ bbox, and connects to torso by only a 2–3px
  brace; it does not cover the head/torso center;
- diagonal polearm has core pixels in grip x41..46/y29..35 and tip x>=60/y<=6, plus corridor boxes
  x47..51/y22..28 and x53..57/y11..18; tip-to-grip distance>=32, horizontal projection>=16,
  screen slope 0.8–2.0; not vertical;
- row2 still passes all R1/R2/R3 metrics.
- every row0/1/3 cell hash stays byte-identical to R3.

GREEN exact pre-rim authored core (canonical right-facing layout; adjust small offsets by authored dir
without changing dimensions):

HEAD/HOOD
- ink outer rectangle/stepped mass exactly 12x12, x29..40,y16..27;
- slate/steel fill, 6px ice visor; connected 2px neck below.

TORSO
- ink outer x27..44,y27..43 (18x17); broad steel/slate chest visible in full;
- shoulder/hip joins but no single vertical bar.

LEGS
- left x27..32 and right x38..43, each exactly 6px wide to y51;
- transparent core gap x33..37 from y45 down; separate 6–8px feet;
- pose1 shifts opposite foot by 1–2px without joining gap.

SHIELD
- camera-near side x9..24,y20..48 (16x29), stepped rectangular corners;
- broad slate/steel faces, bone/ochre clamps, small ice+MAG focus;
- 2–3px arm bridge from shield right edge to torso x27; shield never overlaps x29..40/y16..27.

DIAGONAL POLEARM
- 2–3px connected shaft from grip near (43,33) to tip near (61,4), length>=33px;
- steel/ice shaft and 8–10px crystal head entirely outside torso/shield;
- tip extends >=17px beyond torso right edge; obvious diagonal at normal scale;
- for front/back authored facings retain diagonal with slope 0.8–2.0; never collapse vertical.

After core paint, existing applyCombatExteriorRim adds ice/sky; exact mirror derivation unchanged.
Do not change R3 world scale 1.67x1.92, 2px rim, luma floor, MAG focus size, or other rows.

QA/export:
- `/home/bobbyranka/workspace/evidence/starhaven-vs4a-rift/`
- same 1366x1024 no-UI camera/terrain/positions as R1–R3;
- atlas/contact/lineup/battle/manifest;
- manifest includes row0/1/3 frozen hash pass and row2 anatomy metrics;
- draw calls 5/5, runtime MAG0, sim share<8ms, no errors.

Run:
npm run test:vs4
npm run qa:vs4 -- --out /home/bobbyranka/workspace/evidence/starhaven-vs4a-rift
npm run test:vs3
npm run qa:vs3 -- --out /tmp/vs4a-vs3
npm run test:vs2b
npm run qa:vs2b -- --out /tmp/vs4a-vs2b
npm run test:vs2-ai
npm run qa:vs2-ai-doctrine -- --out /tmp/vs4a-vs2a
npm run qa:progression-handoff -- --out /tmp/vs4a-progress
npm run qa:touch-contract -- --out /tmp/vs4a-touch
npm run test:m0
npm run test:m2
npm run test:m2-ai
npm run test:m3
npm run test:m4
npm run test:m5
npm run build
git diff --check

Commit prefix `VS4A:`. Do not push/deploy. Report RED anatomy failures, frozen-row hashes, row2 metrics,
exports, draw calls, sim share, regressions, SHA, deviations.