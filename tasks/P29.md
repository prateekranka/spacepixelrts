# P29 — Put opening camp on camera flanks

**Builder:** Composer 2.5 · **Gap from:** `tasks/P28-critic.md`

## Problem

P28 hold-fire ranks passed, but forward camps sat on world ±X (screen up/down), hidden under the 56px top bar and 168px command deck. Critic saw repeating purple floor + two gems, no gatherers in frame.

## Changes

- Moved Helion/Kryos houses, workers, gems, rocks, and props onto **Z flanks** (`|x-cx| ≤ 1.6`, `|z-cz|` 5.5–8.5, outside fire lane) in `stampOpeningGround` + `spawnScenario`.
- Helion house + 3 gathering workers + ore node at `(cx, cz−7)`; Kryos house + worker + gas at `(cx, cz+7)`; solar/ore extras at `(cx±1, cz±6)`.
- Dust variants diverged: ochre pebbles, teal cracks, dark crater blob (`src/atlas.ts`).
- `buildMap` stamps 10 hashed 2×2 rock decals on the pad (skip `|z-cz| ≤ 4` fire corridor).
- Probe `0.2.4-wave1`. Hold-fire ranks, house scale ~1.42, infantry scale unchanged.

## Verification

`critic/out/p29.png` — p99 **4.2 ms**, probe **0.2.4-wave1**, 62 ents. Palette top shares include `#554433` (10%) and `#335566` (8.9%) vs single purple mat. Houses/workers/gems visible on screen flanks; two ranks still exchanging fire. Deployed to https://spacepixelrts.pages.dev
