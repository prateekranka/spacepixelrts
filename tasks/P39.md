# P39 — Workers gathering the contested mid gem

**Builder:** Composer 2.5 · **Gap from:** `tasks/P38-critic.md`

## Problem

P38 anchored the duel with a stationary gold gem between two firing wings, but the wow frustum still lacked visible gather activity — workers and camp scrap hugged the margins while the belt read as fighters + center sticker, not a live economy.

## Changes

- **3 Helion workers** at `(cx±0.85, cz-0.7)` and `(cx, cz+0.85)` with `Ord.Gather`, `tx/tz = (cx, cz)` on the contested mid ore.
- **`openingMidGemWorkerEnt`** — freeze separate/return for mid-gem tableau workers while `tick < 240` (camp flank workers unchanged).
- **Opening invulnerability:** bolts skip `Kind.Worker` hits; melee skips workers while `tick < 240` so the gather cluster survives the duel.
- **Camp workers, ranks, dust, mid gem:** unchanged from P38; worker render scale stays **1.55**.

## Verification

`critic/out/p39.png` — p99 **4.2 ms**, probe **0.2.14-wave1**, 69 ents. ≥3 gold-pack Helion workers clustered on center gem between green and blue firing wings; quiet dust and round gap sparks intact. Deployed to https://spacepixelrts.pages.dev
