# P99 — Helion Habitat Builder worker

Completed 2026-08-18 at version `0.9.12-iso`.

## Shipped

- Replaced the Helion worker with the option-5 stocky, bearded Habitat Builder across an 8-direction 32×48 idle/walk strip.
- Kept idle and walk hands empty and made the lime headgear read as a construction hard-hat with a MAG lamp.
- Added extra Helion atlas rows for Build, food cargo, crystal cargo, and worker attack without changing the 512px/16-column atlas contract.
- Wired worker order, cargo type, and 8-direction facing to the new action rows while preserving legacy corpse/dissolve frames and Kryos/Nihiline worker art.
- Gave Build a dedicated braced two-handed mallet strike at a shallow lime footing, with a transverse metal head, motion arc, and impact pixels.

## Verification

- `npx tsc --noEmit` — passed.
- `node scripts/p99-worker-shots.mjs` against the existing `http://127.0.0.1:5173` server — passed.
- Runtime atlas: `512x912`, normal worker rows at `576`, action rows at `720`, row height `48`.
- Fresh blind `cursor-grok-4.6-xhigh` round-4 verdict: **PASS** — the same bearded hard-hat villager reads across empty-handed idle/walk, hammering, produce, crystal, and raised-mallet attack states.
