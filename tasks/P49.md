# P49 — Mixed-arms inbound wave (fighters + siege)

**Builder:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §6 (mixed-arms army pressure) · **Shipped:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Version:** `0.3.5-wave2`

---

## Problem (from P48 critic)

Inbound pressure was **scout-heavy** — five Scouts vs four Fighters, no siege. Real army mass but not the mixed-arms stack that makes AoE2:DE defense interesting.

---

## Changes

| Area | Fix |
|---|---|
| **`stepEnemyMarshal()`** | Tick **240**: enemy instant **Dominion** (`epoch = 2`). Resource floor **≥ 500 ore / 120 gas / 160 charge**; ongoing ore/gas/charge floors. Barracks rally moved to forward pad `(MAP−18, MAP−16)`. |
| **Head-start Siege** | Tick **250**: one Breaker spawned at forward pad, queued for peel (not teleported onto player Hall). |
| **`pumpMarshalTraining()`** | Post-240 Yard queue: **Siege first** (up to 2), then **Fighters** from Yard + Nexus. No Scout production after 240 (existing scouts still peel). |
| **`tryMarshalTrain()`** | Marshal train shortcuts: Fighter **1 s**, Siege **4 s**. |
| **`main.ts`** | `VERSION = 0.3.5-wave2`. |

Opening clash **0–239**, win/lose banners, and player pop/housing unchanged.

---

## Probe (local, port 5174)

| Field | Early (~80) | Late (420) |
|---|---:|---:|
| `version` | `0.3.5-wave2` | `0.3.5-wave2` |
| Helion living fighters | **8** | **8** |
| Enemy peel on player Hall | **0** | **≥ 6** AttackMove |
| Committed Fighters | **0** | **≥ 4** |
| Committed Siege | **0** | **≥ 1** |
| Enemy `epoch` | **0** (Spark) | **2** (Dominion) |
| `p99FrameMs` (critic) | — | **< 22** |

---

## Verification

```bash
npm run build
npx vite --host --port 5174 --strictPort
```

Playwright (reset + step in one `evaluate`):

1. Tick **80**: Helion rank intact, **0** enemy peel toward player Hall.
2. Tick **420**: **≥ 6** team-1 military committed on player Hall, **≥ 4** Fighters, **≥ 1** Siege.

```bash
npm run critic -- --url http://localhost:5174
npx wrangler pages deploy dist --project-name=spacepixelrts
```
