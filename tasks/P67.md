# P67 — Wave 5 integrator kickoff

**Integrator:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §2 §9, `docs/ARCHITECTURE.md`, `PROGRESS.md` · **Judged:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Version:** `0.5.3-wave4`

**Harness:** `npm run critic -- --url https://spacepixelrts.pages.dev --fps-seconds 3 --wait 3`  
**Probes:** `node scripts/p59-probe.mjs`, `node scripts/p62-probe.mjs`, Playwright tick sweep (gitignored)

Wave 1 opening **PASS** (P41). Wave 2 match-arc **PASS** (P49). Wave 3 feel **PASS** (P58). Wave 4 hardening **PASS** (P66). Opening tableau, marshal timing, VFX, culling, Attack-lock, and HUD insets left untouched.

---

## What still works

| # | Question | Result |
|---|---|---|
| 1 | **Opening still wows?** Helion vs Kryos wrecks, sparks, peel, HUD safe? | **Pass** — tick **59–120**: Helion **8** fighters on `Attack`; Kryos **2–7** living + wreck belt; **2** active sparks; `hitSfx` **52**. Screenshot: green rank above gold gem, shattered Kryos below, gather pocket bottom-left, muzzle sparks on belt. P62 peel: ground right-click → **Move** (`pass: true`); hull right-click → **Attack** (`pass: true`). P64 safe-area still wired. |
| 2 | **Match arc** — train, Orbit, mixed-arms inbound, win/lose? | **Pass** — tick **420**: enemy epoch **2** (Dominion); **13** military committed on player Hall (`tid` **359**) — **8** Fighters + **2** Siege + **3** Scouts, all `AttackMove`. Player HUD shows idle-worker pulse, Ore/Vol/Chg/Pop, train/build verbs (Spark baseline). Win path exists: destroy enemy Hall → `winner = 0`, VICTORY banner text present. |
| 3 | **§9 #3** — three civs as different peoples? | **Fail** — probe `civ: ["vespari","aurion"]` only; **0** `voidmarked` entities at any tick (opening → **1800**). Nihiline art/VFX/stats exist in atlas + sim but never spawn in the live 1v1. |
| 4 | **Would you keep playing a full 1v1?** | **Partial** — opening tableau, command deck, and inbound mixed-arms peel are enough to stay past the wow shot and rally a defense; but every match is the same Helion-vs-Kryos mirror, so it reads as a polished skirmish slice, not a civ roster you'd grind like AoE2:DE. |
| 5 | **`npm run critic` p99** | **Pass** — p99 **3.1 ms** (avg **1.44 ms**), **0** frames worse than 45 fps; probe **66** ents, version **`0.5.3-wave4`**. |

**Marginal (not loudest):** surviving Kryos clash fighters drift `Idle` while Helion holds `Attack` (tick **59**: **5/6** Kryos idle; tick **1800**: **8/9** idle) — cosmetic opening diorama slip, deferred from P66. Master palette still loose (**347** distinct quantized colors). Unique unit in opening is Helion-only (**1** `Ravager`); readable at zoom but never exercises Kryos `Prism` or Nihiline `Shade`.

---

## Single biggest Wave 5 gap

**The live 1v1 is hard-coded Helion vs Kryos, so Nihiline (`voidmarked`) — the third people in DESIGN §2 — never appears and §9 #3 (“three civs are different peoples, not recolors”) cannot pass.**

**Smallest independently-judgeable fix:** Add a boot-time civ picker (three tiles: Helion / Kryos / Nihiline) that sets `world.civ[0]` and assigns `world.civ[1]` to a different civ; assert via probe that loading with Nihiline selected yields `civ` including `voidmarked`, purple/green tendril silhouettes on canvas, and at least one `Kind.Shade` or `Kind.UniqueB` entity by tick **420**.

---

## Harness (live)

| Field | Value |
|---|---|
| URL | `https://spacepixelrts.pages.dev` |
| p99FrameMs | **3.1** |
| avgFrameMs | 1.44 |
| framesWorseThan45fps | 0 |
| Probe version | `0.5.3-wave4` |
| Probe ents | **66** |
| Probe tick (opening) | **120** |
| Probe hitSfx | **52** |
| rendererInfo.drawn | **32** |
| Teams pop | **22/30** · **9/30** |
| Civ | vespari vs aurion |
| Console issues | none |

---

## Playwright probes (live)

| Probe | Result |
|---|---|
| Opening tick **59** | Helion **8**, Kryos **6**, Kryos idle **5**, sparks **0**, `voidmarked` ents **0** |
| P62 ground Move | tick **64**, `order` Move, `pass: true` |
| P62 hull Attack | tick **64**, `order` Attack, `tid` matches hull, `pass: true` |
| Skirmish tick **420** | **13** enemy military on Hall **359** (8 Fighter + 2 Siege + 3 Scout), enemy epoch **2** |
| Skirmish tick **1800** | `winner` **−1** (no player input — expected stall); Kryos idle **8/9** |
| Third civ | `voidmarkedSpawnedInV1: false` |
| Win banner | Kill enemy Hall → `winner: 0`, banner text “VICTORY / Enemy Nexus shattered” |
