# P30 — Park flank camps beside the ranks

**Builder:** Composer 2.5 · **Gap from:** `tasks/P29-critic.md`

## Problem

P29 moved camps onto Z flanks but kept them at `|z-cz|=7`, which landed gems and structures in the top-right frustum corner under the FPS readout — still absent from the central belt between HUD bars.

## Changes

- Repositioned Helion/Kryos houses, workers, and gems to `|z-cz| ≈ 5.1–5.9` beside the rank wings (`|x-cx| ≤ 1.2`), deleting all `cz±7` node/prop/house placements.
- Helion: house `(cx+0.2, cz−5.15)`, 3 workers, ore `(cx−0.2, cz−5.85)`; Kryos: house `(cx−0.2, cz+5.15)`, 2 workers, gas `(cx+0.2, cz+5.85)`; solar `(cx+1.0, cz−5.5)`.
- Rocks/props constrained to `|z-cz|` 5.0–6.2 outside fire lane `|z-cz| < 4.4`.
- 3×3 Dust camp pads stamped under each house (no rock).
- House billboard `targetH` **1.75**; worker scale **1.22** (fighters stay 1.08).
- `openingFlankCampEnt()` forces vis for camp ents through tick 240.

## Verification

`critic/out/p30.png` — p99 **6.3 ms**, probe **0.2.5-wave1**, 60 ents. House + 3 workers + gems visible beside Helion wing in central playfield (not under FPS corner); two ranks still exchanging fire. Deployed to https://spacepixelrts.pages.dev
