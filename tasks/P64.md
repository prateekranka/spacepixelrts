# P64 — iPad HUD safe-area insets

**Builder:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §7.2 §7.4, P59 iPad QA gap · **Judged:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Probe:** `0.5.3-wave4`

**Harness:** `node scripts/p64-probe.mjs --url http://127.0.0.1:5174` (or live URL)  
**Output:** `critic/out/p64-probe.json`

---

## What changed

| Area | Result |
|---|---|
| **`#topbar` safe-area** | `padding-top/left/right: env(safe-area-inset-*, 0px)`; `height: calc(56px + env(safe-area-inset-top, 0px))` with `box-sizing: border-box` — content stays 56 px tall; bar grows with notch. |
| **`#bottom` safe-area** | `padding` uses `calc(10px + env(safe-area-inset-*, 0px))` on sides/bottom; `height: calc(168px + env(safe-area-inset-bottom, 0px))` — 44×44 verb tiles stay above home indicator. |
| **Desktop critic** | `env(..., 0px)` fallback — `#bottom` still **168 px**; mid-map clash unchanged. |
| **Opening / marshal** | Untouched — no restage, no Attack-lock or culling changes. |

---

## Playwright probes (5174, Chrome headless)

| Probe | Result |
|---|---|
| Critic 1180×820 | Helion **8**, Kryos **6**; `#bottom` **168 px**; `hudUsesSafeArea` **true**; p99 **3.1 ms** |
| iPad Air landscape | `viewport-fit=cover` **yes**; CSS contains `safe-area-inset`; mocked **34 px** bottom inset → `paddingBottom` **44 px**; verb bottoms **701** vs `innerHeight - inset` **786** — **85 px** clearance |
| `npm run critic` | p99 **3.3 ms**, **0** frames worse than 45 fps |

---

## Ship note (`0.5.3-wave4`)

- `src/hud.ts` — `#topbar` / `#bottom` `env(safe-area-inset-*)` padding + growing height.
- `src/main.ts` — `VERSION = 0.5.3-wave4`.
- `scripts/p64-probe.mjs` — critic layout + iPad inset mock probes.
