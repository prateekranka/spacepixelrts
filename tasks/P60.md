# P60 — Viewport cull instance uploads

**Builder:** Composer 2.5 · **Bar:** `docs/ARCHITECTURE.md` §8, P59 gap · **Judged:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Probe:** `0.5.1-wave4`

**Harness:** `node scripts/p60-probe.mjs --url http://127.0.0.1:5174` (or live URL)  
**Output:** `critic/out/p60-probe.json`

---

## What changed

| Area | Result |
|---|---|
| **Viewport cull** | `GameRenderer.draw()` skips `setMatrixAt` for ents / bolts / sparks whose interpolated `(x, z)` fall outside a world-space AABB derived from `camera.left/right/top/bottom`, look-at ground point, and **≥1 tile** margin. `InstancedMesh.frustumCulled` stays `false`. |
| **Overlay** | HP / foot ellipses already gated by `drawnEntIds` — off-screen ents skip overlay work automatically. Fog / terrain unchanged. |
| **`rendererInfo.drawn`** | `info()` exposes last frame `mesh.count` for `__STARHOLD__.rendererInfo.drawn`. |
| **Opening tableau** | Unchanged visually — in-frustum Helion rank, Kryos wrecks, sparks still upload. |

---

## Playwright probes (5174, Chrome headless)

| Probe | Result |
|---|---|
| Opening 3 s | Helion **8**, Kryos **6**, sparks **2**; `drawn` **44** (not 0); p99 **3.1 ms** |
| +180 Fighters at **(4, 4)**, camera on opening | `drawn` **32** (within **~30** of opening **46**); p99 **4.2 ms** |
| +180 Fighters at **look-at** | `drawn` **218** (+172 vs opening); p99 **4.1 ms** |

---

## Ship note (`0.5.1-wave4`)

- `src/render.ts` — `updateViewBounds()`, cull in entity/bolt/spark loops, `info().drawn`.
- `src/main.ts` — `VERSION = 0.5.1-wave4`.
- `scripts/p60-probe.mjs` — opening / off-screen / on-screen horde probes.
