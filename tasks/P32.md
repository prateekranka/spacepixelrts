# P32 — Separate Helion wing from worker camp

**Builder:** Composer 2.5 · **Gap from:** `tasks/P31-critic.md`

## Problem

P31 parked camp workers at `cz-4.75` while Helion's 4-col formation (`colPitch=1.55`) spanned `cz-4.13` to `cz+0.53`. Workers overlapped the outer combat column, so the critic saw one green corner blob and only the blue wedge fighting.

## Changes

- **Combat formation tightened:** `colPitch` **1.55 → 1.12** (Helion Z span ~`cz-3.48` to `cz-0.12`); `rowPitch` 1.40, `gap` 3.60 unchanged.
- **Camp pushed out:** workers at `(cx - 1.0 + i*0.75, cz ± 5.35)`; ore/gas gem at `(cx + 1.1, cz ± 5.35)`; house at `(cx, cz ± 6.45)` — ~1.87-tile gulf between wing and camp.
- `openingFlankCampEnt()` vis band `|z-cz|` **5.0–6.8**; ground gems/props updated to match.
- Worker **1.55**, house **1.75**, fighters **1.08** unchanged. Freeze-fire + `Ord.Attack` preserved.

## Verification

`critic/out/p32.png` — p99 **4.3 ms**, probe **0.2.7-wave1**, 59 ents. Green and blue fighter ranks exchanging bolts in center; worker+house camp visible upper-right with empty tiles between camp and Helion wing. Deployed to https://spacepixelrts.pages.dev
