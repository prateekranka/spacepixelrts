# P52 — Death dissolve + corpse stain

**Builder:** Composer 2.5 · **Bar:** `docs/DESIGN.md` §5.4 #6 · **Shipped:** 2026-08-16

**Live:** https://spacepixelrts.pages.dev · **Version:** `0.4.1-wave3`

---

## Gap (from P50/P51)

Death was static wreck placement (`hp=0` → frame 4 stain immediately). DESIGN mandates **2-frame dissolve → corpse stain ~1.5s → gone**, not pop-out.

---

## Changes

| Area | Result |
|---|---|
| **Dissolve phase** | `dissolveT` on `Ent`; `kill()` sets `dissolveT = 2 ticks`, then `corpseT = 1.5s`. `stepCorpses` drains dissolve before stain countdown. |
| **Atlas frames 5–6** | Pixel-scatter dissolve sprites (`applyDissolve`); frame 4 remains corpse stain. |
| **Opening decor** | Pre-placed Kryos wrecks keep `dissolveT = 0`, `corpseT = 4` — no dissolve, P41 tableau intact. |
| **Live combat** | Helion/Kryos deaths via `kill()` play dissolve → stain → slot free. Pop skips `hp≤0`, `dissolveT>0`, `corpseT>0`. |
| **Perf** | No per-tick alloc; reuses existing entity pool. |

**Not in scope (P53):** foot ellipses, HP-bar combat gate.

---

## Verify

| Check | Result |
|---|---|
| `npm run build` | Pass |
| Opening screenshot wait 3s | Helion rank vs Kryos wreck belt + mid gem intact |
| Force-kill via `__STARHOLD_WORLD__` | Dissolve frames 5→6, then stain frame 4, then slot freed |
| `npm run critic` local 5174 | p99 < 22 ms |
| Deploy | `spacepixelrts.pages.dev` ships `0.4.1-wave3` |

---

## Files

- `src/engine.ts` — `DISSOLVE_DUR`, `STAIN_DUR`, `dissolveT` on `Ent`
- `src/sim.ts` — `kill()` dissolve chain, `stepCorpses` two-phase, `recountPop` gate
- `src/atlas.ts` — frames 5–6 dissolve, `applyDissolve`
- `src/render.ts` — `uvFor` dissolve frame pick
- `src/main.ts` — `VERSION = 0.4.1-wave3`
